## What changed

<!-- One or two sentences. What does this do, and why? -->

Closes #

## How to check it

<!-- What should a reviewer click through? Name the list URL, the panel, or
     the preview page if the change is visible. -->

1.

## Checks

- [ ] `npm run check` passes locally.
- [ ] Tests cover the new behavior, or the bug fix has a test that failed before.
- [ ] The README, `CONTRIBUTING.md`, or `server/README.md` is updated if behavior or commands changed.
- [ ] `CHANGELOG.md` has an entry under Unreleased, unless this is internal only.

## Token, permissions, and privacy

- [ ] No token is sent anywhere except `api.github.com`.
- [ ] `permissions` and `host_permissions` in `public/manifest.json` are unchanged, or the reason is explained below.
- [ ] No analytics, telemetry, or third-party script was added.
- [ ] Nothing new is sent to the shared cache beyond public repository counters.

<!-- If you ticked an exception above, explain it here. -->

## Screenshots

<!-- For UI changes. Before and after if you have both. -->
