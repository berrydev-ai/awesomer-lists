{
  "name": "awesomer-lists",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "npm run test && npm run typecheck && npm run build && npm run verify",
    "dev": "node scripts/preview.mjs --hot",
    "preview": "node scripts/preview.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "verify": "node scripts/check-dist.mjs"
  },
  "devDependencies": {
    "@fontsource/geist": "5.2.9",
    "@fontsource/geist-mono": "5.2.8",
    "@types/chrome": "0.2.2",
    "esbuild": "0.28.1",
    "happy-dom": "20.10.6",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
